 export default function Cars(Props){

        return (
        <p>i am a {Props.name}</p>
        );

  //when using props for numbers  and boolean store the props in a curly braces else use  Quotation marks
  //props is basically setting value of a component function in another components and writting code for its usuaiblity
  //almost does the same function with the "this " keyword in vanilla js
    
    
}
